# Presency — local secrets for Clark (Cursor agent)
# Copy to .env.local and fill in. NEVER commit .env.local.

# ── Supabase (project mhtqvuglujodpffpmmhe) ─────────────────────
# Dashboard → Project Settings → API
SUPABASE_URL=https://mhtqvuglujodpffpmmhe.supabase.co
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ── Make.com (org 7601650 / team 2271692) ─────────────────────────
# Profile → API → Create token
MAKE_API_TOKEN=
MAKE_TEAM_ID=2271692
MAKE_ORG_ID=7601650

# ── Twilio (for Team Variables — do not hardcode in blueprints) ───
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# ── Stripe — use Cursor Stripe plugin OAuth, or: ──────────────────
STRIPE_SECRET_KEY=

# ── Cloudflare (account 977beef08266ebbfcf90d6252baec592) ─────────
# My Profile → API Tokens → Edit zone DNS + Cache Purge
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ZONE_ID=

# ── Anthropic (review response engine) ────────────────────────────
ANTHROPIC_API_KEY=
